// Update this page (the content is just a fallback if you fail to update the page)

const Index = () => {
  // Redirect to home to prevent blank page
  window.location.replace('/');
  return null;
};

export default Index;
